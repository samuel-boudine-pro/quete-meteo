/**
 * Composant MeteoAdvanced :
 * va etre un rectangle qui va afficher trois parties : 
 * 
 * 1 : une View avec deux éléments enfants -> pour afficher "Aube" + heure de lever du soleil
 * 2 : une View avec deux éléments enfants -> pour afficher "Crépuscule" + heure de coucher du soleil
 * 3 : une View avec deux éléments enfants ->  pour afficher "Vent" + vitesse du vent
 * 
 * Ce n'est qu'un composant d'affichage ! (il faudra donc lui envoyer les bonnes données pour nos trois affichages ...)
 */

function MeteoAdvanced() {
    return (
        <></>
    )
}